import sys
from scapy.layers.inet import IP, TCP
from scapy.all import *
from ipaddress import IPv4Address
from random import getrandbits
import random
import telnetlib

if __name__ == '__main__':
    ip = IP(dst="") # Fill this in
    SYN = TCP(dport=, sport=, flags=, seq=) # Fill this in
    response = sr1(ip/SYN)
    print(response.show())
